Esta carpeta se genera localmente.

verify-all.txt se sobrescribe en cada verificacion.
jar-contents.txt se regenera al inspeccionar el paquete.
tools/ y downloads/ contienen el Maven preparado por mvnw.cmd.

Solo este README pertenece al repositorio. Los demas archivos de .local
son evidencia/diagnostico de cada maquina y no se versionan.
